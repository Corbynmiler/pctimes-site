{
  "name": "pctimes-site",
  "version": "0.1.0",
  "private": true,
  "description": "Pohutukawa Coast Times — community newspaper website",
  "scripts": {
    "start": "eleventy --serve --quiet",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "js-yaml": "^4.1.1",
    "markdown-it": "^14.1.1"
  }
}
